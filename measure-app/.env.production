# ============================================================
# 生产环境配置
# ============================================================

# 后端 API 地址（改成你服务器的真实域名）
# - 方案A（国内中转）：填写国内中转服务器域名，中转服务器再转发到香港
# - 方案B（直连香港）：填写香港服务器域名（国内用户需科学上网）
VITE_API_BASE=https://你的域名.com

# ============================================================
# CDN 加速配置（可选，用于国内用户访问前端静态资源加速）
# 使用方式：
#   1. 将 npm run build 产物上传到 OSS（阿里云/腾讯云 COS）
#   2. 开启 CDN 并绑定自定义加速域名
#   3. 将 CDN 加速域名填入下方（必须带协议头）
# 注意：填写后必须重新执行 npm run build 才生效
# 不使用 CDN 则注释掉此行（保持相对路径 /）
# VITE_CDN_BASE=https://cdn.你的域名.com

